name: Float major version tag

on:
  release:
    types: [published]

jobs:
  tag:
    runs-on: ubuntu-slim
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: ./float-major-tag
